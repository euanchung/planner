# 과제 리마인더 — Vercel 설치 안내

Supabase 콘솔은 안 들어가도 된다. SQL도 없다. 전부 Vercel 화면 안에서 끝난다.

순서는 **① GitHub에 파일 올리기 → ② Vercel에 연결 → ③ 저장소 붙이기 → ④ 폰에 설치** 이다. 처음 한 번만 20~30분 걸리고, 그 뒤로 친구들은 주소만 열면 된다.

---

## ① GitHub에 파일 올리기

Vercel은 Netlify처럼 폴더를 끌어다 놓는 방식이 없고 GitHub 저장소를 읽어간다. 프로그램 설치 없이 웹에서 다 할 수 있다.

1. github.com 에 가입한다.
2. 오른쪽 위 **+** → **New repository**
   - Repository name: `planner`
   - **Public** 으로 둔다 (Private도 되지만 Public이 설정이 덜 까다롭다)
   - **Create repository**
3. 다음 화면에서 **uploading an existing file** 링크를 누른다.
4. 압축을 푼 폴더 안의 **내용물**을 전부 끌어다 놓는다. 폴더째가 아니라 안에 있는 것들이다.

```
index.html
manifest.json
sw.js
package.json
icon-192.png
icon-512.png
icon-maskable.png
api/  ← 폴더 (auth.js, state.js, admin.js)
lib/  ← 폴더 (db.js)
```

`api` 와 `lib` 폴더가 빠지면 로그인이 안 된다. 드래그가 잘 안 되면 폴더를 하나씩 끌어다 놓아도 된다.

5. 아래 **Commit changes** 를 누른다.

---

## ② Vercel에 연결

1. vercel.com 에 **GitHub 계정으로** 가입한다. (같은 계정을 써야 저장소가 바로 보인다)
2. **Add New… → Project**
3. 방금 만든 `planner` 저장소 옆의 **Import**
4. 설정은 아무것도 건드리지 않고 **Deploy**

1분쯤 뒤 `https://planner-무작위.vercel.app` 주소가 나온다. 지금 열면 화면은 뜨지만 로그인은 아직 안 된다. 저장소를 아직 안 붙였기 때문이다.

---

## ③ 저장소 붙이기 (여기가 핵심)

1. 프로젝트 화면 위쪽 탭에서 **Storage**
2. **Create Database** 또는 **Browse Marketplace**
3. 목록에서 **Upstash** 를 고르고 그 안에서 **Redis** 를 고른다
   - 이름은 아무거나, Region은 **가까운 곳(도쿄/서울)** 으로
   - 요금제는 **Free** 를 고른다
4. 만들고 나면 **Connect Project** 로 `planner` 프로젝트를 연결한다. 이미 연결돼 있으면 그대로 두면 된다.
5. **다시 배포해야 값이 들어간다.** 왼쪽 **Deployments** 탭 → 맨 위 배포의 오른쪽 **⋯** → **Redeploy** → **Redeploy**

이제 주소를 열면 로그인 화면이 뜨고 **첫 번째로 가입한 사람이 자동으로 관리자**가 된다. 네가 제일 먼저 가입하면 된다.

> **Upstash가 안 보이면**: Marketplace 검색창에 `redis` 를 쳐본다. Redis를 제공하는 다른 곳(Redis Cloud 등)을 골라도 되지만, 그 경우 `KV_REST_API_URL` / `KV_REST_API_TOKEN` 이라는 이름의 값이 자동으로 안 들어갈 수 있다. Upstash가 가장 확실하다.

---

## ④ 폰에 설치

**아이폰(사파리)** — 주소를 연다 → 아래 공유 버튼 → **홈 화면에 추가**
**안드로이드(크롬)** — 주소를 연다 → 오른쪽 위 점 세 개 → **앱 설치** (또는 화면 위 '앱으로 설치' 버튼)

아이콘이 생기고 주소창 없이 전체화면으로 뜬다. 인터넷이 끊겨도 열리고, 다시 연결되면 알아서 맞춰진다. 친구들에게는 주소만 보내면 되고, 각자 가입해서 쓰면 서로 안 보인다.

---

## 나중에 앱을 고칠 때

GitHub 저장소에서 파일을 수정하고 Commit 하면 Vercel이 알아서 다시 배포한다. 따로 할 일은 없다.

---

## 알아둘 점

**관리자** — 첫 가입자에게 자동으로 붙는다. 관리 탭에서 친구들의 미완료·기한 지난 개수와 마지막 저장 시각이 보인다. 과제 제목은 안 보이게 해뒀지만 서버에는 남으니, 친구들에게 "관리자는 개수를 볼 수 있다"고 미리 말해두는 게 맞다.

**비밀번호** — 서버에 저장할 때 원문이 아니라 되돌릴 수 없는 형태로 바꿔 저장한다. 그래도 은행 비밀번호 같은 걸 재활용하지는 말자.

**알림** — 앱이 열려 있을 때 정해진 시각에 울린다. 앱이 완전히 꺼져 있을 때도 울리게 하려면 서버가 시각에 맞춰 신호를 보내는 장치가 더 필요하다. Upstash에 QStash라는 예약 발송 기능이 있어서 나중에 붙일 수 있다. 지금은 폰 기본 알람을 19시 30분 반복으로 하나 맞춰두고 울리면 앱을 여는 게 가장 확실하다.

**무료 한도** — Upstash 무료 요금제는 하루 요청 수 한도가 넉넉해서, 몇 명이 매일 쓰는 정도로는 근처에도 못 간다. Vercel Hobby도 개인 용도면 무료다.

**막혔을 때** — 로그인 화면에서 "저장소가 연결되지 않았어"가 뜨면 ③번의 Redeploy를 안 한 것이다. "서버 오류 404"가 뜨면 `api` 폴더가 GitHub에 안 올라간 것이다.
